import 'package:flutter/material.dart';

class PlantHealthCheckScreen extends StatelessWidget {
  const PlantHealthCheckScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Plant Health Check"),
      ),
    );
  }
}
