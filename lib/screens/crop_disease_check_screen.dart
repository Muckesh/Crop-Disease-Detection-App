import 'package:flutter/material.dart';

class CropDiseaseCheckScreen extends StatelessWidget {
  const CropDiseaseCheckScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Plant Health Check"),
      ),
    );
  }
}
